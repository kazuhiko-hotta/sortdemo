# SortDemo 開発指示書

あなたはGNOMEデスクトップアプリケーション開発のエキスパートです。
PythonとGTK 4 (Libadwaita) を使用して、ソートアルゴリズムを可視化するアプリケーション「SortDemo」を開発してください。

## プロジェクト概要
色々なソートアルゴリズムをグラフィカルに表示し、動作原理を視覚的に理解できるデモアプリケーション。

## 技術スタック
- **言語**: Python 3.13+
- **GUI**: GTK 4 + Libadwaita (PyGObject)
- **管理**: pyproject.toml

## 機能要件

### 1. ユーザーインターフェース (UI)
- **ヘッダーバー**:
  - アルゴリズム選択（ドロップダウン）
  - コントロールボタン（開始、一時停止、リセット）
- **メイン描画エリア**:
  - 配列の要素を棒グラフとして描画
  - 状態に応じた色分け（比較中、交換中、確定済みなど）
  - **初期状態**: アプリ起動時に、ランダムにシャッフルされた数列（バー）が表示されていること。
- **設定パネル**:
  - 描画速度（アニメーションウェイト）の調整スライダー
  - 要素数の調整スライダー（範囲: 10 〜 1000程度を想定、最大65535まで対応可能な設計）
- **ステータス表示**:
  - 現在のステップ数（比較回数、アクセス回数）

### 2. 実装アルゴリズム
以下のアルゴリズムを実装し、容易に追加可能な設計にすること。
- バブルソート
- 挿入ソート
- クイックソート
- マージソート

### 3. 実装方針
- **非同期描画**:
  - ソートロジックはジェネレータ (`yield`) を使用して実装する。
  - `GLib.timeout_add` 等を利用して、UIスレッドをブロックせずにアニメーションを更新する。
- **モダンなUI**:
  - Libadwaitaのウィジェットを活用し、GNOME Human Interface Guidelines (HIG) に準拠する。
